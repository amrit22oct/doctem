{% comment %} import React, { useState } from "react";
import Header from "../../organisms/Header";

const AppHeader = () => {
  return (
    <header className="w-full bg-[var(--primary)] shadow-lg flex  justify-between items-center  h-[88px]  rounded-b-2xl">
      <Header />
    </header>
  );
};

export default AppHeader; {% endcomment %}
